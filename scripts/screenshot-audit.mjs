/**
 * Baseline / verification screenshots + axe color-contrast (WCAG-oriented).
 * Alias of contrast-audit — run: `node scripts/screenshot-audit.mjs`
 * (with `npm run dev` already listening on AUDIT_BASE_URL).
 */
import "./contrast-audit.mjs";
